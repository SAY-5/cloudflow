{{- if .Values.postgres.enabled }}
apiVersion: v1
kind: Secret
metadata:
  name: postgres-credentials
  labels:
    {{- include "cloudflow.labels" $ | nindent 4 }}
type: Opaque
stringData:
  password: {{ .Values.postgres.password | default "changeme" | quote }}
{{- end }}
